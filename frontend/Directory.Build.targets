<Project>
  <PropertyGroup>
    <WindowsTargetPlatformVersion>10.0.22621.0</WindowsTargetPlatformVersion>
    <CppWinRTPath>Y:\.nuget\packages\microsoft.windows.cppwinrt\2.0.230706.1\bin\</CppWinRTPath>
    <ReactNativeWindowsDir Condition="'$(ReactNativeWindowsDir)' == ''">$(MSBuildThisFileDirectory)node_modules\react-native-windows\</ReactNativeWindowsDir>
  </PropertyGroup>
  <ItemDefinitionGroup>
    <ClCompile>
      <PrecompiledHeader>NotUsing</PrecompiledHeader>
      <MultiProcessorCompilation>false</MultiProcessorCompilation>
    </ClCompile>
  </ItemDefinitionGroup>
  <!-- ReferencePath to source-build WinMD removed.
       Using NuGet package (UseExperimentalNuget=true) which provides Microsoft.ReactNative.winmd automatically. -->
  <Target Name="_CppWinRTCleanMakeReferenceProjectionOutputs" />
  <Target Name="CopyPrebuiltPriFiles" BeforeTargets="_GenerateProjectPriFileCore">
    <ItemGroup>
      <!-- Only include the .pri file if it exists (source build). Skipped when using NuGet packages. -->
      <PrebuiltPriFiles Include="$(ReactNativeWindowsDir)target\ReactCommon\ReactCommon.pri"
                        Condition="Exists('$(ReactNativeWindowsDir)target\ReactCommon\ReactCommon.pri')" />
    </ItemGroup>
    <Copy SourceFiles="@(PrebuiltPriFiles)" DestinationFiles="@(PrebuiltPriFiles->'$(ReactNativeWindowsDir)target\$(Platform)\$(Configuration)\ReactCommon\ReactCommon.pri')" SkipUnchangedFiles="true" Condition="'@(PrebuiltPriFiles)' != ''" />
  </Target>
</Project>
